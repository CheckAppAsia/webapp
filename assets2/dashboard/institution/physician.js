$(document).ready(function(){
	
	/*_ajax('dashboard/institution/services/removeService',{
		physician_id: $(this).data('id'),
		mirror: $(this).data('id')
	},function(response){
		if(response.status!='TRUE'){
			$("#service_"+response.mirror).slideDown();
		}
	},'POST');*/
	
});